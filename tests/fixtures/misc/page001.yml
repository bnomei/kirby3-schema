title: Intro
blueprint: page

create:
  title:
    label: Product name
  fields:
    - price
    - brand
  redirect: false
  status: draft

options:
  preview: "{{ site.url }}?preview={{ page.slug }}"

fields:
  autoplay:
    type: toggle
    default: true
    translate: false
    width: 1/3
    text:
      - play on click
      - autoplay


